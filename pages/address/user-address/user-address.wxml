
<!--地址管理-->
<radio-group class="radio-group" wx:for="{{address}}">
<view hidden="{{hiddenAddress}}" class="address" >
  <view class="address-icon" bindtap="setDefault" data-id="{{item.address_id}}">
    <radio checked="{{item.moren==1?true:false}}" value="{{index}}" />
  </view>
  
  <view class="address-detail">
    <view class="address-name-phone">
      <text class="address-name">{{item.consignee}}</text>
      <text class="address-phone">{{item.mobile}}</text>
    </view>
    <view class="address-info">{{item.address}}</view>
    <view class="address-edit">
      <view>
        <icon></icon>
        <text hidden="{{item.is_default==0?false:true}}"></text>
      </view>
      <view>
        <text hidden="{{item.is_default==0?false:true}}" bindtap="setDefault" data-id="{{item.address_id}}">设置默认</text>
        <text hidden="{{item.is_default==0?false:true}}"> | </text>
        <text data-id="{{item.address_id}}" bindtap="delAddress">删除</text>
      </view>
    </view>
  </view>
</view>
</radio-group>
<navigator url="../address?cartId={{cartId}}&g_id={{g_id}}&tuanid={{tuanid}}&tuan_sn={{tuan_sn}}">
  <view hidden="{{hiddenAddress}}" class="add-address">
    <image class="add-address-icon" src="#"></image>
    <view>新增地址</view>
  </view>
</navigator>